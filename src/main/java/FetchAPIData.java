public class FetchAPIData {
    // responsible for connection with api and retrieving data (JSON format)

    // set up connection
    // make sure you can retrieve the JSON data
    // have the retrieved data displayed in the log as a test


}
